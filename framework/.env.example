# MonoPHP Environment Configuration
# Copy this file to .env and update the values

# Required: Your site domain (e.g., myapp.com or localhost:8000)
SITE_DOMAIN=localhost:8000

# Optional: Google OAuth (only needed for googleoauth variant)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=
